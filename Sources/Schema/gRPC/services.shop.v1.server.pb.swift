/*
 * DO NOT EDIT.
 *
 * Generated by the protocol buffer compiler.
 * Source: shop/v1/ShopService_v1.proto
 *
 */

/*
 * Copyright 2017, gRPC Authors All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
import Foundation
import Dispatch
import gRPC
import SwiftProtobuf

/// Type for errors thrown from generated server code.
internal enum Services_Shop_V1_ShopServerError : Error {
  case endOfStream
}

/// To build a server, implement a class that conforms to this protocol.
internal protocol Services_Shop_V1_ShopProvider {
  func ping(request : Services_Shop_V1_Ping_Request, session : Services_Shop_V1_ShopPingSession) throws -> Services_Shop_V1_Ping_Response
  func shopinfo(request : Services_Shop_V1_ShopInfo_Request, session : Services_Shop_V1_ShopShopInfoSession) throws -> Services_Shop_V1_ShopInfo_Response
  func enrollmember(request : Services_Shop_V1_EnrollMember_Request, session : Services_Shop_V1_ShopEnrollMemberSession) throws -> Services_Shop_V1_EnrollMember_Response
  func checkzipcode(request : Services_Shop_V1_CheckZipcode_Request, session : Services_Shop_V1_ShopCheckZipcodeSession) throws -> Services_Shop_V1_CheckZipcode_Response
  func verifymember(request : Services_Shop_V1_VerifyMember_Request, session : Services_Shop_V1_ShopVerifyMemberSession) throws -> Services_Shop_V1_VerifyMember_Response
  func submitorder(request : Services_Shop_V1_SubmitOrder_Request, session : Services_Shop_V1_ShopSubmitOrderSession) throws -> Services_Shop_V1_SubmitOrder_Response
  func getorder(request : Services_Shop_V1_GetOrder_Request, session : Services_Shop_V1_ShopGetOrderSession) throws -> Services_Shop_V1_GetOrder_Response
}

/// Common properties available in each service session.
internal class Services_Shop_V1_ShopSession {
  fileprivate var handler : gRPC.Handler
  internal var requestMetadata : Metadata { return handler.requestMetadata }

  internal var statusCode : Int = 0
  internal var statusMessage : String = "OK"
  internal var initialMetadata : Metadata = Metadata()
  internal var trailingMetadata : Metadata = Metadata()

  fileprivate init(handler:gRPC.Handler) {
    self.handler = handler
  }
}

// Ping (Unary)
internal class Services_Shop_V1_ShopPingSession : Services_Shop_V1_ShopSession {
  private var provider : Services_Shop_V1_ShopProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Shop_V1_ShopProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Shop_V1_Ping_Request(serializedData:requestData)
        let replyMessage = try self.provider.ping(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// ShopInfo (Unary)
internal class Services_Shop_V1_ShopShopInfoSession : Services_Shop_V1_ShopSession {
  private var provider : Services_Shop_V1_ShopProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Shop_V1_ShopProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Shop_V1_ShopInfo_Request(serializedData:requestData)
        let replyMessage = try self.provider.shopinfo(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// EnrollMember (Unary)
internal class Services_Shop_V1_ShopEnrollMemberSession : Services_Shop_V1_ShopSession {
  private var provider : Services_Shop_V1_ShopProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Shop_V1_ShopProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Shop_V1_EnrollMember_Request(serializedData:requestData)
        let replyMessage = try self.provider.enrollmember(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// CheckZipcode (Unary)
internal class Services_Shop_V1_ShopCheckZipcodeSession : Services_Shop_V1_ShopSession {
  private var provider : Services_Shop_V1_ShopProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Shop_V1_ShopProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Shop_V1_CheckZipcode_Request(serializedData:requestData)
        let replyMessage = try self.provider.checkzipcode(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// VerifyMember (Unary)
internal class Services_Shop_V1_ShopVerifyMemberSession : Services_Shop_V1_ShopSession {
  private var provider : Services_Shop_V1_ShopProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Shop_V1_ShopProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Shop_V1_VerifyMember_Request(serializedData:requestData)
        let replyMessage = try self.provider.verifymember(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// SubmitOrder (Unary)
internal class Services_Shop_V1_ShopSubmitOrderSession : Services_Shop_V1_ShopSession {
  private var provider : Services_Shop_V1_ShopProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Shop_V1_ShopProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Shop_V1_SubmitOrder_Request(serializedData:requestData)
        let replyMessage = try self.provider.submitorder(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// GetOrder (Unary)
internal class Services_Shop_V1_ShopGetOrderSession : Services_Shop_V1_ShopSession {
  private var provider : Services_Shop_V1_ShopProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Shop_V1_ShopProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Shop_V1_GetOrder_Request(serializedData:requestData)
        let replyMessage = try self.provider.getorder(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}


/// Main server for generated service
internal class Services_Shop_V1_ShopServer {
  private var address: String
  private var server: gRPC.Server
  private var provider: Services_Shop_V1_ShopProvider?

  /// Create a server that accepts insecure connections.
  internal init(address:String,
              provider:Services_Shop_V1_ShopProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    self.server = gRPC.Server(address:address)
  }

  /// Create a server that accepts secure connections.
  internal init?(address:String,
               certificateURL:URL,
               keyURL:URL,
               provider:Services_Shop_V1_ShopProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    guard
      let certificate = try? String(contentsOf: certificateURL, encoding: .utf8),
      let key = try? String(contentsOf: keyURL, encoding: .utf8)
      else {
        return nil
    }
    self.server = gRPC.Server(address:address, key:key, certs:certificate)
  }

  /// Start the server.
  internal func start(queue:DispatchQueue = DispatchQueue.global()) {
    guard let provider = self.provider else {
      fatalError() // the server requires a provider
    }
    server.run {(handler) in
      print("Server received request to " + handler.host
        + " calling " + handler.method
        + " from " + handler.caller
        + " with " + String(describing:handler.requestMetadata) )

      do {
        switch handler.method {
        case "/services.shop.v1.Shop/Ping":
          try Services_Shop_V1_ShopPingSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.shop.v1.Shop/ShopInfo":
          try Services_Shop_V1_ShopShopInfoSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.shop.v1.Shop/EnrollMember":
          try Services_Shop_V1_ShopEnrollMemberSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.shop.v1.Shop/CheckZipcode":
          try Services_Shop_V1_ShopCheckZipcodeSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.shop.v1.Shop/VerifyMember":
          try Services_Shop_V1_ShopVerifyMemberSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.shop.v1.Shop/SubmitOrder":
          try Services_Shop_V1_ShopSubmitOrderSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.shop.v1.Shop/GetOrder":
          try Services_Shop_V1_ShopGetOrderSession(handler:handler, provider:provider).run(queue:queue)
        default:
          break // handle unknown requests
        }
      } catch (let error) {
        print("Server error: \(error)")
      }
    }
  }
}
