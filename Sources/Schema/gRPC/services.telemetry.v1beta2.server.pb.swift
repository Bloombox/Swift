/*
 * DO NOT EDIT.
 *
 * Generated by the protocol buffer compiler.
 * Source: telemetry/v1beta2/TelemetryService_Beta2.proto
 *
 */

/*
 * Copyright 2017, gRPC Authors All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
import Foundation
import Dispatch
import gRPC
import SwiftProtobuf

/// Type for errors thrown from generated server code.
internal enum Services_Telemetry_V1Beta2_EventTelemetryServerError : Error {
  case endOfStream
}

/// To build a server, implement a class that conforms to this protocol.
internal protocol Services_Telemetry_V1Beta2_EventTelemetryProvider {
  func ping(request : Services_Telemetry_V1beta2_TelemetryPing_Request, session : Services_Telemetry_V1Beta2_EventTelemetryPingSession) throws -> Services_Telemetry_V1beta2_TelemetryPing_Response
  func event(request : Services_Telemetry_V1beta2_Event_Request, session : Services_Telemetry_V1Beta2_EventTelemetryEventSession) throws -> Google_Protobuf_Empty
  func batch(request : Services_Telemetry_V1beta2_Event_BatchRequest, session : Services_Telemetry_V1Beta2_EventTelemetryBatchSession) throws -> Services_Telemetry_V1beta2_TelemetryResponse
  func error(request : Services_Telemetry_V1beta2_Exception, session : Services_Telemetry_V1Beta2_EventTelemetryErrorSession) throws -> Google_Protobuf_Empty
}

/// Common properties available in each service session.
internal class Services_Telemetry_V1Beta2_EventTelemetrySession {
  fileprivate var handler : gRPC.Handler
  internal var requestMetadata : Metadata { return handler.requestMetadata }

  internal var statusCode : Int = 0
  internal var statusMessage : String = "OK"
  internal var initialMetadata : Metadata = Metadata()
  internal var trailingMetadata : Metadata = Metadata()

  fileprivate init(handler:gRPC.Handler) {
    self.handler = handler
  }
}

// Ping (Unary)
internal class Services_Telemetry_V1Beta2_EventTelemetryPingSession : Services_Telemetry_V1Beta2_EventTelemetrySession {
  private var provider : Services_Telemetry_V1Beta2_EventTelemetryProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Telemetry_V1Beta2_EventTelemetryProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Telemetry_V1beta2_TelemetryPing_Request(serializedData:requestData)
        let replyMessage = try self.provider.ping(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// Event (Unary)
internal class Services_Telemetry_V1Beta2_EventTelemetryEventSession : Services_Telemetry_V1Beta2_EventTelemetrySession {
  private var provider : Services_Telemetry_V1Beta2_EventTelemetryProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Telemetry_V1Beta2_EventTelemetryProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Telemetry_V1beta2_Event_Request(serializedData:requestData)
        let replyMessage = try self.provider.event(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// Batch (Unary)
internal class Services_Telemetry_V1Beta2_EventTelemetryBatchSession : Services_Telemetry_V1Beta2_EventTelemetrySession {
  private var provider : Services_Telemetry_V1Beta2_EventTelemetryProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Telemetry_V1Beta2_EventTelemetryProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Telemetry_V1beta2_Event_BatchRequest(serializedData:requestData)
        let replyMessage = try self.provider.batch(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// Error (Unary)
internal class Services_Telemetry_V1Beta2_EventTelemetryErrorSession : Services_Telemetry_V1Beta2_EventTelemetrySession {
  private var provider : Services_Telemetry_V1Beta2_EventTelemetryProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Telemetry_V1Beta2_EventTelemetryProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Telemetry_V1beta2_Exception(serializedData:requestData)
        let replyMessage = try self.provider.error(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}


/// Main server for generated service
internal class Services_Telemetry_V1Beta2_EventTelemetryServer {
  private var address: String
  private var server: gRPC.Server
  private var provider: Services_Telemetry_V1Beta2_EventTelemetryProvider?

  /// Create a server that accepts insecure connections.
  internal init(address:String,
              provider:Services_Telemetry_V1Beta2_EventTelemetryProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    self.server = gRPC.Server(address:address)
  }

  /// Create a server that accepts secure connections.
  internal init?(address:String,
               certificateURL:URL,
               keyURL:URL,
               provider:Services_Telemetry_V1Beta2_EventTelemetryProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    guard
      let certificate = try? String(contentsOf: certificateURL, encoding: .utf8),
      let key = try? String(contentsOf: keyURL, encoding: .utf8)
      else {
        return nil
    }
    self.server = gRPC.Server(address:address, key:key, certs:certificate)
  }

  /// Start the server.
  internal func start(queue:DispatchQueue = DispatchQueue.global()) {
    guard let provider = self.provider else {
      fatalError() // the server requires a provider
    }
    server.run {(handler) in
      print("Server received request to " + handler.host
        + " calling " + handler.method
        + " from " + handler.caller
        + " with " + String(describing:handler.requestMetadata) )

      do {
        switch handler.method {
        case "/services.telemetry.v1beta2.EventTelemetry/Ping":
          try Services_Telemetry_V1Beta2_EventTelemetryPingSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.telemetry.v1beta2.EventTelemetry/Event":
          try Services_Telemetry_V1Beta2_EventTelemetryEventSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.telemetry.v1beta2.EventTelemetry/Batch":
          try Services_Telemetry_V1Beta2_EventTelemetryBatchSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.telemetry.v1beta2.EventTelemetry/Error":
          try Services_Telemetry_V1Beta2_EventTelemetryErrorSession(handler:handler, provider:provider).run(queue:queue)
        default:
          break // handle unknown requests
        }
      } catch (let error) {
        print("Server error: \(error)")
      }
    }
  }
}

/// Type for errors thrown from generated server code.
internal enum Services_Telemetry_V1Beta2_CommercialTelemetryServerError : Error {
  case endOfStream
}

/// To build a server, implement a class that conforms to this protocol.
internal protocol Services_Telemetry_V1Beta2_CommercialTelemetryProvider {
  func impression(request : Services_Telemetry_V1beta2_CommercialEvent_Impression, session : Services_Telemetry_V1Beta2_CommercialTelemetryImpressionSession) throws -> Google_Protobuf_Empty
  func view(request : Services_Telemetry_V1beta2_CommercialEvent_View, session : Services_Telemetry_V1Beta2_CommercialTelemetryViewSession) throws -> Google_Protobuf_Empty
  func action(request : Services_Telemetry_V1beta2_CommercialEvent_Action, session : Services_Telemetry_V1Beta2_CommercialTelemetryActionSession) throws -> Google_Protobuf_Empty
}

/// Common properties available in each service session.
internal class Services_Telemetry_V1Beta2_CommercialTelemetrySession {
  fileprivate var handler : gRPC.Handler
  internal var requestMetadata : Metadata { return handler.requestMetadata }

  internal var statusCode : Int = 0
  internal var statusMessage : String = "OK"
  internal var initialMetadata : Metadata = Metadata()
  internal var trailingMetadata : Metadata = Metadata()

  fileprivate init(handler:gRPC.Handler) {
    self.handler = handler
  }
}

// Impression (Unary)
internal class Services_Telemetry_V1Beta2_CommercialTelemetryImpressionSession : Services_Telemetry_V1Beta2_CommercialTelemetrySession {
  private var provider : Services_Telemetry_V1Beta2_CommercialTelemetryProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Telemetry_V1Beta2_CommercialTelemetryProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Telemetry_V1beta2_CommercialEvent_Impression(serializedData:requestData)
        let replyMessage = try self.provider.impression(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// View (Unary)
internal class Services_Telemetry_V1Beta2_CommercialTelemetryViewSession : Services_Telemetry_V1Beta2_CommercialTelemetrySession {
  private var provider : Services_Telemetry_V1Beta2_CommercialTelemetryProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Telemetry_V1Beta2_CommercialTelemetryProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Telemetry_V1beta2_CommercialEvent_View(serializedData:requestData)
        let replyMessage = try self.provider.view(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// Action (Unary)
internal class Services_Telemetry_V1Beta2_CommercialTelemetryActionSession : Services_Telemetry_V1Beta2_CommercialTelemetrySession {
  private var provider : Services_Telemetry_V1Beta2_CommercialTelemetryProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Telemetry_V1Beta2_CommercialTelemetryProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Telemetry_V1beta2_CommercialEvent_Action(serializedData:requestData)
        let replyMessage = try self.provider.action(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}


/// Main server for generated service
internal class Services_Telemetry_V1Beta2_CommercialTelemetryServer {
  private var address: String
  private var server: gRPC.Server
  private var provider: Services_Telemetry_V1Beta2_CommercialTelemetryProvider?

  /// Create a server that accepts insecure connections.
  internal init(address:String,
              provider:Services_Telemetry_V1Beta2_CommercialTelemetryProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    self.server = gRPC.Server(address:address)
  }

  /// Create a server that accepts secure connections.
  internal init?(address:String,
               certificateURL:URL,
               keyURL:URL,
               provider:Services_Telemetry_V1Beta2_CommercialTelemetryProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    guard
      let certificate = try? String(contentsOf: certificateURL, encoding: .utf8),
      let key = try? String(contentsOf: keyURL, encoding: .utf8)
      else {
        return nil
    }
    self.server = gRPC.Server(address:address, key:key, certs:certificate)
  }

  /// Start the server.
  internal func start(queue:DispatchQueue = DispatchQueue.global()) {
    guard let provider = self.provider else {
      fatalError() // the server requires a provider
    }
    server.run {(handler) in
      print("Server received request to " + handler.host
        + " calling " + handler.method
        + " from " + handler.caller
        + " with " + String(describing:handler.requestMetadata) )

      do {
        switch handler.method {
        case "/services.telemetry.v1beta2.CommercialTelemetry/Impression":
          try Services_Telemetry_V1Beta2_CommercialTelemetryImpressionSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.telemetry.v1beta2.CommercialTelemetry/View":
          try Services_Telemetry_V1Beta2_CommercialTelemetryViewSession(handler:handler, provider:provider).run(queue:queue)
        case "/services.telemetry.v1beta2.CommercialTelemetry/Action":
          try Services_Telemetry_V1Beta2_CommercialTelemetryActionSession(handler:handler, provider:provider).run(queue:queue)
        default:
          break // handle unknown requests
        }
      } catch (let error) {
        print("Server error: \(error)")
      }
    }
  }
}

/// Type for errors thrown from generated server code.
internal enum Services_Telemetry_V1Beta2_IdentityTelemetryServerError : Error {
  case endOfStream
}

/// To build a server, implement a class that conforms to this protocol.
internal protocol Services_Telemetry_V1Beta2_IdentityTelemetryProvider {
  func action(request : Services_Telemetry_V1beta2_IdentityEvent_Action, session : Services_Telemetry_V1Beta2_IdentityTelemetryActionSession) throws -> Google_Protobuf_Empty
}

/// Common properties available in each service session.
internal class Services_Telemetry_V1Beta2_IdentityTelemetrySession {
  fileprivate var handler : gRPC.Handler
  internal var requestMetadata : Metadata { return handler.requestMetadata }

  internal var statusCode : Int = 0
  internal var statusMessage : String = "OK"
  internal var initialMetadata : Metadata = Metadata()
  internal var trailingMetadata : Metadata = Metadata()

  fileprivate init(handler:gRPC.Handler) {
    self.handler = handler
  }
}

// Action (Unary)
internal class Services_Telemetry_V1Beta2_IdentityTelemetryActionSession : Services_Telemetry_V1Beta2_IdentityTelemetrySession {
  private var provider : Services_Telemetry_V1Beta2_IdentityTelemetryProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Services_Telemetry_V1Beta2_IdentityTelemetryProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Services_Telemetry_V1beta2_IdentityEvent_Action(serializedData:requestData)
        let replyMessage = try self.provider.action(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}


/// Main server for generated service
internal class Services_Telemetry_V1Beta2_IdentityTelemetryServer {
  private var address: String
  private var server: gRPC.Server
  private var provider: Services_Telemetry_V1Beta2_IdentityTelemetryProvider?

  /// Create a server that accepts insecure connections.
  internal init(address:String,
              provider:Services_Telemetry_V1Beta2_IdentityTelemetryProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    self.server = gRPC.Server(address:address)
  }

  /// Create a server that accepts secure connections.
  internal init?(address:String,
               certificateURL:URL,
               keyURL:URL,
               provider:Services_Telemetry_V1Beta2_IdentityTelemetryProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    guard
      let certificate = try? String(contentsOf: certificateURL, encoding: .utf8),
      let key = try? String(contentsOf: keyURL, encoding: .utf8)
      else {
        return nil
    }
    self.server = gRPC.Server(address:address, key:key, certs:certificate)
  }

  /// Start the server.
  internal func start(queue:DispatchQueue = DispatchQueue.global()) {
    guard let provider = self.provider else {
      fatalError() // the server requires a provider
    }
    server.run {(handler) in
      print("Server received request to " + handler.host
        + " calling " + handler.method
        + " from " + handler.caller
        + " with " + String(describing:handler.requestMetadata) )

      do {
        switch handler.method {
        case "/services.telemetry.v1beta2.IdentityTelemetry/Action":
          try Services_Telemetry_V1Beta2_IdentityTelemetryActionSession(handler:handler, provider:provider).run(queue:queue)
        default:
          break // handle unknown requests
        }
      } catch (let error) {
        print("Server error: \(error)")
      }
    }
  }
}
